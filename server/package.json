{
  "name": "pokemon-showdown-move-simulator",
  "version": "1.0.0",
  "scripts": {
    "run": "node dist/src/index.js",
    "setup": "npm run cleanup && npm run copy && npm run tsc",
    "tsc": "tsc",
    "build": "npm run cleanup:build && tsc -p tsconfig.prod.json",
    "watch": "tsc-watch",
    "copy": "node build/copyDependenciesToLib.js",
    "cleanup": "npm run cleanup:ps && npm run cleanup:build",
    "cleanup:ps": "rimraf src/pokemon-showdown-lib",
    "cleanup:build": "rimraf .aws-sam/build/GenerateMovesets"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.51",
    "@types/node": "^13.13.5",
    "fs-extra": "^9.0.0",
    "prepend-file": "^1.3.1",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3",
    "walk": "^2.3.14"
  }
}
