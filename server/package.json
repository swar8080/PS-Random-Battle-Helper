{
  "name": "pokemon-showdown-move-simulator",
  "version": "1.0.0",
  "scripts": {
    "run": "node dist/src/index.js",
    "setup": "npm run cleanup && npm run copy && npm run tsc",
    "tsc": "tsc",
    "watch": "tsc-watch",
    "copy": "node build/copyDependenciesToLib.js",
    "cleanup": "npm run cleanup:src && npm run cleanup:dist",
    "cleanup:src": "rimraf lib",
    "cleanup:dist": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^13.13.5",
    "fs-extra": "^9.0.0",
    "prepend-file": "^1.3.1",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3",
    "walk": "^2.3.14"
  }
}
